#!/usr/bin/env puma

directory '<%= @directory %>'
rackup '<%= @directory %>/config.ru'
environment '<%= @environment %>'
pidfile '<%= @puma_pids_dir %>/puma.pid'
state_path '<%= @puma_state_dir %>/puma.state'
threads 1, 16
port 8080

on_restart do
  puts 'On restart...'
end
